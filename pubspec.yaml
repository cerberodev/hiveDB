name: helloworld
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^1.4.4+1
  hive_flutter: ^0.3.1

dev_dependencies:
  hive_generator: ^0.8.1
  build_runner: ^1.10.4
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
